#include "shadowsocks/network/relay/ss_tcp_relay.h"


// when stream before calling
void SsTcpRelay::before(SsTcpRelay::StreamCallback callback) {
}

// when stream after calling
void SsTcpRelay::after(SsTcpRelay::StreamCallback callback) {
}
